# Frontend environment variables for Vite
# Copy this file to ".env.local" at the project root and adjust values as needed.
# IMPORTANT: Vite only exposes variables prefixed with VITE_ to the frontend.

# Toggle backend integration (set to true when your backend is ready)
VITE_BACKEND_ENABLED=false

# Base URL of your backend API (no trailing slash)
# Examples:
# - Local: http://localhost:3000
# - Production: https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:3000

# After creating .env.local or changing values:
# 1) Stop the dev server (Ctrl+C)
# 2) Run: npm run dev
# Then check the browser console for:
# [ENV] VITE_BACKEND_ENABLED: true/false
# [ENV] BACKEND_ENABLED: true/false
# [ENV] VITE_API_BASE_URL: http://...
# [ENV] API_BASE_URL: http://...
