# Backend environment variables for Parently minimal Express + MySQL server
# Copy this file to server/.env and fill in the values.

# OPTION A: Single connection string (RECOMMENDED)
# Add your MySQL connection string here:
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: mysql://root:password@127.0.0.1:3306/parently
DATABASE_URL=

# OPTION B: Individual connection parameters (use these ONLY if DATABASE_URL is empty)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_DATABASE=

# Frontend origin for CORS (Dev default is Vite on 5173)
FRONTEND_ORIGIN=http://localhost:5173

# Server listening port
PORT=3000

# Table name that stores the waitlist (change if your table name differs)
# Your table must contain columns: first_name, last_name, email, phone_number, country, id (AUTO_INCREMENT)
WAITLIST_TABLE=waitlist_subscribers
