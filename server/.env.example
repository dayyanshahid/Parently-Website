# Express + MySQL backend environment variables (example)
# Copy this file to "server/.env" and fill in real values.
# IMPORTANT: Do NOT commit server/.env to Git. It's already ignored.

# Option A) Single connection string (preferred)
# Format: mysql://USER:PASS@HOST:PORT/DBNAME
DATABASE_URL=

# Option B) Individual parameters (used if DATABASE_URL is empty)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_DATABASE=parently_db

# Server
PORT=3000

# CORS: set to your frontend origin (dev)
FRONTEND_ORIGIN=http://localhost:5173

# MySQL table name for waitlist entries
WAITLIST_TABLE=wait_list
